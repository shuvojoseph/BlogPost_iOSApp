# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BlogPost_iOSApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BlogPost_iOSApp

    # Podfile
    platform :ios, '13.0' # Or your desired iOS platform version use_frameworks!

    pod 'Alamofire', '~> 5.10.2' # Or the latest stable version

  target 'BlogPost_iOSAppUITests' do
    # Pods for testing
  end

end
